{#<h3>Welcome to Hello Bird</h3>#}
{#Hello {{ user.fullName }} ! You've registered into Hello Bird !<br><br>#}

{#Please confirm account by clicking this link : <a href="{{ url('security_confirm',{'token':user.confirmationToken}) }}"#}
{#to login, go to <a href="{{ url('security_login') }}">Login page</a><br><br>#}
{#Thanx !1#}
{{ 'email.registration'|trans({
    '%name%': user.fullName,
    '%confirm-link%': url('security_confirm', {'token': user.confirmationToken}),
    '%login-link%': url('security_login')
})|raw }}